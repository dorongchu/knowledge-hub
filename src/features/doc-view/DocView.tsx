/** 문서뷰 자리표시자. TipTap 에디터 + 노드 트리는 "문서뷰" 단계에서 구현. */
export function DocView() {
  return (
    <div className="flex h-full items-center justify-center text-sm text-muted-foreground">
      문서뷰 (TipTap) — 구현 예정
    </div>
  )
}
